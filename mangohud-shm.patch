diff --git a/src/meson.build b/src/meson.build
index 9534a18..0a77201 100644
--- a/src/meson.build
+++ b/src/meson.build
@@ -95,6 +95,7 @@ if is_unixy
     'net.cpp',
     'shell.cpp',
     'ftrace.cpp',
+    'shm.c'
   )
 
   if get_option('with_fex')
diff --git a/src/overlay.cpp b/src/overlay.cpp
index 2196953..4cee366 100644
--- a/src/overlay.cpp
+++ b/src/overlay.cpp
@@ -28,6 +28,9 @@
 #include "net.h"
 #include "fex.h"
 #include "ftrace.h"
+extern "C" {
+   #include "shm.h"
+}
 
 #ifdef __linux__
 #include <libgen.h>
@@ -312,6 +315,26 @@ void update_hud_info_with_frametime(struct swapchain_stats& sw_stats, const stru
 void update_hud_info(struct swapchain_stats& sw_stats, const struct overlay_params& params, uint32_t vendorID){
    uint64_t now = os_time_get_nano(); /* ns */
    uint64_t frametime_ns = now - sw_stats.last_present_time;
+
+   MangoHudSHM shm_data = {};
+
+   shm_data.metrics.fps = sw_stats.fps;
+   shm_data.metrics.frametime = frametime;
+   shm_data.metrics.min_frametime = min_frametime;
+   shm_data.metrics.max_frametime = max_frametime;
+   shm_data.metrics.cpu_percent = cpuStats.GetCPUDataTotal().percent;
+
+   if (gpus && gpus->active_gpu()) {
+      shm_data.metrics.gpu_load = gpus->active_gpu()->metrics.load;
+   } else {
+      shm_data.metrics.gpu_load = 0;
+   }
+
+   shm_data.param_enabled[PARAM_GPU_USAGE] = params.enabled[OVERLAY_PARAM_ENABLED_gpu_stats];
+   shm_data.param_enabled[PARAM_CPU_USAGE] = params.enabled[OVERLAY_PARAM_ENABLED_cpu_stats];
+
+   shm_update(&shm_data);
+
    if (!get_params()->no_display || logger->is_active())
       update_hud_info_with_frametime(sw_stats, params, vendorID, frametime_ns);
 }
diff --git a/src/shm.c b/src/shm.c
new file mode 100644
index 0000000..c525cee
--- /dev/null
+++ b/src/shm.c
@@ -0,0 +1,54 @@
+#include <sys/mman.h>
+#include <sys/stat.h>
+#include <fcntl.h>
+#include <unistd.h>
+#include <string.h>
+#include <stddef.h>
+#include "shm.h"
+
+static MangoHudSHM* data = NULL;
+static bool initialized = false;
+
+__attribute__((destructor))
+static void cleanup(void) {
+    if (data) {
+        munmap(data, sizeof(MangoHudSHM));
+        data = NULL;
+    }
+    shm_unlink("/MangoHud");
+}
+
+static void init(void) {
+    if (initialized) return;
+
+    int fd = shm_open("/MangoHud", O_CREAT | O_RDWR, 0666);
+    if (fd >= 0) {
+        ftruncate(fd, sizeof(MangoHudSHM));
+
+        data = (MangoHudSHM*)mmap(NULL, sizeof(MangoHudSHM), PROT_READ | PROT_WRITE, MAP_SHARED, fd, 0);
+        
+        if (data == MAP_FAILED) {
+            data = NULL;
+        }
+        close(fd);
+    }
+    initialized = true;
+}
+
+void shm_update(const MangoHudSHM* shm_data) {
+    if (!initialized) {
+        init();
+    }
+
+    if (!data || !shm_data) return;
+
+    data->update_count++;
+    __atomic_thread_fence(__ATOMIC_RELEASE);
+
+    memcpy(data->param_enabled, shm_data->param_enabled, sizeof(bool) * PARAM_ENABLED_MAX);
+    
+    data->metrics = shm_data->metrics;
+
+    __atomic_thread_fence(__ATOMIC_RELEASE); 
+    data->update_count++; 
+}
diff --git a/src/shm.h b/src/shm.h
new file mode 100644
index 0000000..0d61c31
--- /dev/null
+++ b/src/shm.h
@@ -0,0 +1,26 @@
+#pragma once
+#include <stdint.h>
+#include <stdbool.h>
+
+typedef enum {
+    PARAM_GPU_USAGE,
+    PARAM_CPU_USAGE,
+    PARAM_ENABLED_MAX
+} MetricFlag;
+
+typedef struct {
+    float fps;
+    float frametime;
+    double min_frametime;
+    double max_frametime;
+    float cpu_percent;
+    int32_t gpu_load;
+} MangoHudMetrics;
+
+typedef struct {
+    uint64_t update_count;
+    bool param_enabled[PARAM_ENABLED_MAX];
+    MangoHudMetrics metrics;
+} MangoHudSHM;
+
+void shm_update(const MangoHudSHM* shm_data);
